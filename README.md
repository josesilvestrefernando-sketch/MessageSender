# MessageSender
A simple messaging page built using Apache Tomcat and Java Servlets. It is sending messages to another player, but operates entirely within a Local Area Network (LAN) environment.  The application does not use any real email protocols or external mail servers.
